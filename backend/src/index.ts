// src/index.ts
console.log("🚀 DeFi Yield Vault Backend is running!");

const vaultName: string = "Automated Yield Vault";
const apyThreshold: number = 1.5;

console.log(`Vault: ${vaultName}`);
console.log(`Rebalance threshold: ${apyThreshold}%`);